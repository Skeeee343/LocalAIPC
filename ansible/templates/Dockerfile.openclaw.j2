{# LocalAIPC: upstream OpenClaw image + ffmpeg (Discord reply pipeline shells out to it). #}
FROM {{ openclaw_image }}
USER root
RUN apt-get update && apt-get install -y --no-install-recommends ffmpeg && rm -rf /var/lib/apt/lists
USER node
